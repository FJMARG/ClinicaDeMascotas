import { RegistroMascotasService } from './registro-mascotas-service';

describe('RegistroMascotasService', () => {
  it('should create an instance', () => {
    expect(new RegistroMascotasService()).toBeTruthy();
  });
});
