import { ConfigFichaPublica } from './config-ficha-publica';

describe('ConfigFichaPublica', () => {
  it('should create an instance', () => {
    expect(new ConfigFichaPublica()).toBeTruthy();
  });
});
